export interface ConceptDisplayConfigurationRepository {

    //TODO LPDC-916: add method findById()
    //TODO LPDC-916: implement sparql repo (add bestuurseenheid as a required parameter of the spqrql constructor; and then ask to that bestuurseenheid the user graph: Iri (which builds it up) - because these reside in a user graph)

}